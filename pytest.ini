[pytest]
testpaths = tests
# Surface the deprecation noise the app filters at runtime, but don't let
# third-party warnings fail the suite.
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
addopts = -q --strict-markers
